<UserControl x:Class="UtilityWpf.View.ListBoxUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         
             mc:Ignorable="d" 
   
             Background="White"  Name="usercontrol">

    <!--DataContext="{DynamicResource a33}"-->

    <UserControl.Resources>
        <ResourceDictionary  Source="/UtilityWpf.View;component/Themes/ListBoxItemStyle.xaml"/>
    </UserControl.Resources>



        <!--<dg2d:HorizontalPanel     x:Name="HPanel"  ItemsSource="{Binding}"  >-->


        <ListBox  x:Name="HPanel"  ItemsSource="{Binding Items}" 
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                            ScrollViewer.HorizontalScrollBarVisibility="Auto"
            ItemContainerStyle="{StaticResource HorizontalPanelItemStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ContentPresenter Content="{Binding Object}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>


</UserControl>