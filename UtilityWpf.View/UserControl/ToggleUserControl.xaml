<UserControl x:Class="UtilityWpf.View.ToggleUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
   d:DesignHeight="300" d:DesignWidth="300"  Name="usercontrol" >
    <StackPanel HorizontalAlignment="Center" Margin="5">

        <UniformGrid>
            <Label Content="{Binding Title}"></Label>

            <ToggleButton HorizontalAlignment="Center" Margin="5" Padding="5" Width="80" IsChecked="{Binding Input}">
                <ToggleButton.Style>
                    <Style TargetType="{x:Type ToggleButton}">
                        <Setter Property="Content" Value="{Binding UnChecked,TargetNullValue='UnChecked'}"/>
                        <Style.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Content" Value="{Binding Checked,TargetNullValue='Checked'}"/>
                                <Setter Property="Foreground" Value="Red"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>
        </UniformGrid>

    </StackPanel>

</UserControl>
